{
  "name": "payflow-saas",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "check": "npm run check -w server && npm run lint -w client && npm run build -w client",
    "test": "npm test -w server && npm test -w client",
    "release:archive": "bash scripts/create-release.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "eslint": "10.8.0"
  },
  "overrides": {
    "eslint": "10.8.0"
  }
}
